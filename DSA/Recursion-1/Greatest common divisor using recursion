class Result {
    static int gcd(int i, int j) {
        // Base case: if j is 0, return i
        if (j == 0) {
            return i;
        }
        // Recursive case: call gcd with j and the remainder of i divided by j
        return gcd(j, i % j);
    }
}
